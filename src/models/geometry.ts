/**
 * GoogleMapsPlatform
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Location, locationSchema } from './location.js';
import { Viewport, viewportSchema } from './viewport.js';

/** An object describing the location. */
export interface Geometry {
  location: Location;
  viewport: Viewport;
  additionalProperties?: Record<string, unknown>;
}

export const geometrySchema: Schema<Geometry> = lazy(() =>
  typedExpandoObject(
    {
      location: ['location', locationSchema],
      viewport: ['viewport', viewportSchema],
    },
    'additionalProperties',
    optional(unknown())
  )
);
