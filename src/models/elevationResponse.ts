/**
 * GoogleMapsPlatform
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { ElevationResult, elevationResultSchema } from './elevationResult.js';
import { ElevationStatus, elevationStatusSchema } from './elevationStatus.js';

export interface ElevationResponse {
  /** When the service returns a status code other than `OK`, there may be an additional `error_message` field within the response object. This field contains more detailed information about thereasons behind the given status code. This field is not always returned, and its content is subject to change. */
  errorMessage?: string;
  status: ElevationStatus;
  results: ElevationResult[];
  additionalProperties?: Record<string, unknown>;
}

export const elevationResponseSchema: Schema<ElevationResponse> = lazy(() =>
  typedExpandoObject(
    {
      errorMessage: ['error_message', optional(string())],
      status: ['status', elevationStatusSchema],
      results: ['results', array(elevationResultSchema)],
    },
    'additionalProperties',
    optional(unknown())
  )
);
